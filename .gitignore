# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Data — never commit the dataset
data/raw/
data/processed/
*.zip
*.tar
*.tar.gz

# Results: track tables (small) and figures (small) in git so we have
# a reproducible record of every reported number alongside the code.
# Large artifacts (checkpoints) stay gitignored — see *.pt below.

# Model checkpoints
*.pt
*.pth
*.ckpt
checkpoints/

# Logs
*.log
runs/
wandb/
mlruns/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Claude local settings (personal, not team-wide)
.claude/settings.local.json
.claude/scheduled_tasks.lock
